import { AppContext } from '@/components/context'
import {useContext} from 'react'

const Pagination = () =>{
    return(
        <div>
            hello
        </div>
    )
}
export default Pagination;